# ══════════════════════════════════════════════════════════════════════════════
#  .env.railway.template — Railway.app environment variables
#
#  How to use:
#    Option A — Railway CLI (set once per service):
#      railway variables set ANTHROPIC_API_KEY=sk-ant-...
#      railway variables set NEWSAPI_KEY=your-key
#
#    Option B — Railway Dashboard:
#      Project → your-service → Variables → RAW Editor
#      Paste this file, replace placeholder values, save.
#
#    Option C — Bulk set via deploy script:
#      cp .env.railway.template .env.railway
#      (fill in real values)
#      ./deployment/deploy_railway.sh  ← reads .env.railway automatically
#
#  DO NOT commit .env.railway to git.
# ══════════════════════════════════════════════════════════════════════════════

# ── Required: AI & news ────────────────────────────────────────────────────────
# Get your Claude API key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# Get your NewsAPI key (free tier) at: https://newsapi.org/register
NEWSAPI_KEY=your_newsapi_key_here

# ── Database ───────────────────────────────────────────────────────────────────
# DO NOT set DATABASE_URL manually.
# Railway injects this automatically when you add a PostgreSQL plugin:
#   Project → New Service → Database → PostgreSQL
# If you're using an external PostgreSQL:
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# ── Optional: Alpha Vantage (fallback price data) ─────────────────────────────
# Free tier at: https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_KEY=

# ── Optional: Telegram alerts ─────────────────────────────────────────────────
# 1. Create bot: message @BotFather on Telegram → /newbot
# 2. Get chat ID: send a message to your bot, then visit:
#    https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── Dashboard URL (shown in Telegram alert messages) ──────────────────────────
# Set this AFTER Railway assigns a domain to your web service.
# Find it: Railway Dashboard → web service → Settings → Domain
DASHBOARD_URL=https://your-app.up.railway.app

# ── Deployment environment ─────────────────────────────────────────────────────
ENVIRONMENT=production

# ── Health check server ────────────────────────────────────────────────────────
# Port for the FastAPI /health endpoint (separate from Streamlit's $PORT).
# Set this as a variable on the HEALTH service only (not web/worker/monitor).
HEALTH_PORT=8080

# ── Paper trading account size (USD) ─────────────────────────────────────────
# Overrides the balance in config/watchlist.yaml.
ACCOUNT_BALANCE=10000.0
