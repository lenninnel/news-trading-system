# ══════════════════════════════════════════════════════════════════
#  News Trading System — Environment Variables Template
#  Copy this file to .env and fill in your values.
#  NEVER commit .env to git.
# ══════════════════════════════════════════════════════════════════

# ── Required: AI & news ────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...
NEWSAPI_KEY=your_newsapi_key_here

# ── Database ───────────────────────────────────────────────────────
# Local SQLite (default — leave blank to use news_trading.db):
DB_PATH=news_trading.db
# Production PostgreSQL (Railway/Render set this automatically):
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# ── Telegram notifications (optional) ─────────────────────────────
# 1. Create a bot: https://t.me/BotFather
# 2. Send a message to the bot, then visit:
#    https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── Dashboard ─────────────────────────────────────────────────────
# URL shown as inline button in Telegram messages:
DASHBOARD_URL=http://localhost:8501

# ── Deployment environment ─────────────────────────────────────────
# "development" or "production"
ENVIRONMENT=development

# ── Health check server ────────────────────────────────────────────
# Port for the /health endpoint (separate from Streamlit's $PORT):
HEALTH_PORT=8080

# ── Account settings ──────────────────────────────────────────────
# Override the balance set in config/watchlist.yaml:
# ACCOUNT_BALANCE=10000
