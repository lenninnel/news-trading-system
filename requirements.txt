# ── Core AI & data ────────────────────────────────────────────────────────────
anthropic
requests
python-dotenv
yfinance
ta
pyyaml
numpy>=1.24
pandas>=2.0

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit
streamlit-autorefresh
plotly

# ── Scheduler ─────────────────────────────────────────────────────────────────
schedule

# ── Database ──────────────────────────────────────────────────────────────────
# psycopg2-binary is only needed in production (PostgreSQL).
# For local SQLite development it is optional.
psycopg2-binary>=2.9.11
filelock
msgpack

# ── Broker / execution ──────────────────────────────────────────────────────
alpaca-trade-api>=3.2.0
alpha_vantage==2.3.1
# websockets>=13.0 required by yfinance; alpaca-trade-api declares <11 but
# works fine with the REST API at >=13 (no streaming used)
websockets>=13.0

# ── Macro data ───────────────────────────────────────────────────────────────
fredapi

# ── Health check server ───────────────────────────────────────────────────────
fastapi
uvicorn[standard]

# ── Testing ───────────────────────────────────────────────────────────────────
# Test deps moved to requirements-test.txt — NEVER install in production.
# pip install -r requirements-test.txt
