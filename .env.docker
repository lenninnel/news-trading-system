# ══════════════════════════════════════════════════════════════════
#  .env.docker — Docker local testing environment
#
#  Usage:
#    cp .env.docker .env     # use as your local Docker env
#    ./deployment/test_docker_local.sh
#
#  For a FULL scheduler cycle test (real news + sentiment):
#    Set real values for ANTHROPIC_API_KEY and NEWSAPI_KEY below,
#    then run: FULL_TEST=1 ./deployment/test_docker_local.sh
#
#  NEVER commit .env to git (it is in .gitignore).
# ══════════════════════════════════════════════════════════════════

# ── Required for full scheduler cycle (real API calls) ────────────
# Leave as-is to run infra-only test (no real API calls needed).
# Replace with real keys for: FULL_TEST=1 ./deployment/test_docker_local.sh
ANTHROPIC_API_KEY=sk-ant-dummy-key-for-local-docker-testing
NEWSAPI_KEY=dummy_newsapi_key_for_local_docker_testing

# ── Optional: Alpha Vantage (fallback price source) ───────────────
ALPHA_VANTAGE_KEY=

# ── Optional: Telegram notifications ─────────────────────────────
# 1. Create a bot at https://t.me/BotFather
# 2. Get chat ID: https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ── Dashboard ─────────────────────────────────────────────────────
DASHBOARD_URL=http://localhost:8501

# ── Deployment environment ─────────────────────────────────────────
ENVIRONMENT=development

# ── Health check server port ──────────────────────────────────────
HEALTH_PORT=8080

# ── Database ──────────────────────────────────────────────────────
# DATABASE_URL is set automatically by docker-compose.yml to the
# local PostgreSQL container. Only override if using an external DB.
# DATABASE_URL=postgresql://trader:trader_dev@localhost:5432/news_trading

# ── Account settings ──────────────────────────────────────────────
# Uncomment to override config/watchlist.yaml:
# ACCOUNT_BALANCE=10000
